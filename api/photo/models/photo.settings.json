{
  "kind": "collectionType",
  "connection": "default",
  "collectionName": "photos",
  "info": {
    "name": "photo"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "Images": {
      "collection": "file",
      "via": "related",
      "plugin": "upload",
      "required": false
    }
  }
}
